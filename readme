__________________________________________________________________________________

第一步：启动后端 API 服务 (FastAPI)
确保你的后端接口正常运行，因为大屏需要从 http://127.0.0.1:8000 获取统计和预测数据。
在项目的根目录下，运行以下命令启动后端：

uvicorn backend.main:app --reload

(注意：如果终端显示 Application startup complete.，说明后端已经成功运行在 8000 端口。请保持这个终端不要关闭)

第二步：查看 Streamlit 监控后台 (dashboard.py)

streamlit run dashboard/dashboard.py --server.port 8501

运行后，你的默认浏览器会自动打开一个网页。


第三步 终端 3：启动实时的“引擎”（数据模拟器）

python mock_data/realtime_simulator.py


第四步：
python -m http.server 5500
这行命令的意思是：把当前目录作为一个网站跑起来，端口是 5500。


登陆页面：
http://127.0.0.1:5500/data_screen/login.html


___________________________________________________________________________________
更新 GitHub 的正确操作
第一步：添加修改

git add .

第二步：提交修改

git commit -m "update dashboard and data logic"

这一句的作用是：给这次修改做一个版本记录

说明文字可以随便写，例如：
update dashboard UI
fix parking data bug
update prediction logic

第三步：推送到 GitHub

git push

执行完刷新 GitHub 页面就能看到更新。

github的项目地址：

https://github.com/camellia117/smart_parking_system.git


——————————————————————————————————————————————
快速看 SSH key,可以用,Windows 会直接用记事本打开，复制更方便。

notepad %USERPROFILE%\.ssh\id_ed25519.pub
——————————————————————————————————————————————
